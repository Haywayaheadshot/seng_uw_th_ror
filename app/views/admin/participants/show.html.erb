<%= render 'shared/nav' %>
<h1 class="text-2xl font-bold mb-4">Participant: <%= @participant.name %></h1>
<p><strong>Name:</strong> <%= @participant.name %></p>
<p><strong>Age:</strong> <%= @participant.age %></p>
<%= link_to 'Edit', edit_admin_participant_path(@participant), class: 'text-blue-500 hover:underline' %>
<%= link_to 'Back', admin_participants_path, class: 'text-blue-500 hover:underline ml-2' %>